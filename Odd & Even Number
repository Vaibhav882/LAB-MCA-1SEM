#include <stdio.h>
int main() {
    int a;
    printf("Enter a Number ");
    scanf ("%d", &a);
    if (a%2==0) {
        printf(" entar a odd number " );
    }
        else {
            printf ( " enter a even number ");
        }
    
        return 0;
}

